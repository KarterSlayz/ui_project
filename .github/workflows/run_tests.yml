name: Python autotests
  
on:
  workflow_dispatch:
    inputs:
      deployment_target:
        description: choice test set
        required: true
        type: choice
        options:
         - all
         - smoke
         - switching_page
         - sorted_items
         - rules_passwd
         - message_empty_field
         - button_to_women_sale
         - sale_off

jobs:
  run-tests:
    runs-on: ubuntu-latest
    name: Run autotests
    steps:
      - name: Checkout autotests
        uses: actions/checkout@v4
      - name: Set up Python environment
        uses: actions/setup-python@v5
        with:
          python-version: 'pypy3.10'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run all tests
        if: "github.event.inputs.deployment_targer == 'all'"
        run: pytest
      - name: Run smoke tests
        if: "github.event.inputs.deployment_targer == 'smoke'"
        run: pytest -m smoke
      - name: Run switching_page tests
        if: "github.event.inputs.deployment_targer == 'switching_page'"
        run: pytest -m switching_page
      - name: Run sorted_items tests
        if: "github.event.inputs.deployment_targer == 'sorted_items'"
        run: pytest -m sorted_items
      - name: Run rules_passwd tests
        if: "github.event.inputs.deployment_targer == 'rules_passwd'"
        run: pytest -m rules_passwd
      - name: Run message_empty_field tests
        if: "github.event.inputs.deployment_targer == 'message_empty_field'"
        run: pytest -m message_empty_field
      - name: Run button_to_women_sale tests
        if: "github.event.inputs.deployment_targer == 'button_to_women_sale'"
        run: pytest -m button_to_women_sale
      - name: Run sale_off tests
        if: "github.event.inputs.deployment_targer == 'sale_off'"
        run: pytest -m sale_off
